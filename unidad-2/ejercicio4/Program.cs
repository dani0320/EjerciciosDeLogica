using System;

namespace ejercicio4
{
    class Program
    {
        static void Main(string[] args)
        {
            // Una casa de computación paga a sus empleados un sueldo fijo de ARS15000 más una comisión del 5% sobre el total facturado por cada empleado. Hacer un programa para ingresar el total facturado por un empleado y que luego calcule y emita por pantalla el sueldo total a cobrar por el mismo. // 
            // ejercicio 4 //
            
            // int total, comision, SF; 
            // const int sueldoFijo = 20000; // const = variables que no cambian. 
            // const int porcentajeComision = 0.07F; 

            // Console.WriteLine("Ingresar el total facturado: "); 
            // total = int.Parse(Console.ReadLine()); 

            // comision = total * porcentajeComision; 
            // SF =  comision + sueldoFijo; 

            // Console.WriteLine("Sueldo total: " + SF + " Pesos" ); 

        }
    }
}
